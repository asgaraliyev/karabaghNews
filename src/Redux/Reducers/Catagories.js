const initialState = [
  "World",
  "Economy",
  "Science",
  "Politics",
  "Technology",
  "Sport",
  "Business",
];

export const Catagories=(state=initialState,action)=>{
  if(action.type=="CHANGE_CATAGRIES"){
    return state;
  }else{
    return state;
  }
}
